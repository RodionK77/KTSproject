package com.github.rodionk77.common.database

import android.content.Context
import androidx.room.Room

/*actual fun createDatabase(context: Context): AppDatabase {
    val context = context.applicationContext // передать через AppContainer
    return Room.databaseBuilder(context, AppDatabase::class.java, "app_database.db").build()
}*/